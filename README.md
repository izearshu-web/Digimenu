# The Spice Route — Menu & Loyalty

A Vite + React + Tailwind project wrapping the `RestaurantLoyaltyApp` component.

## Run locally
```bash
npm install
npm run dev
```

## Build for production
```bash
npm run build
```
